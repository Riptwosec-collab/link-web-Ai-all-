<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Link Hub - AI Personal Web Library</title>
    
    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Phosphor Icons -->
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Tailwind Configuration -->
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: { sans: ['Inter', 'sans-serif'] },
                    colors: {
                        brand: {
                            dark: '#07070a',
                            panel: '#111218',
                            glass: 'rgba(25, 26, 35, 0.6)',
                            border: 'rgba(255, 255, 255, 0.08)',
                            accent: '#3b82f6',
                            glow: '#0ea5e9',
                            purple: '#a855f7' // Added for AI categorization highlight
                        }
                    }
                }
            }
        }
    </script>

    <!-- Custom CSS -->
    <style>
        body {
            background-color: #050508;
            background-image: 
                radial-gradient(circle at 50% 0%, rgba(14, 165, 233, 0.15) 0%, transparent 50%),
                linear-gradient(to bottom, transparent, #050508);
            color: #e2e8f0;
            overflow: hidden;
        }

        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.2); }

        .glass {
            background: var(--tw-colors-brand-glass);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--tw-colors-brand-border);
        }

        .glass-card {
            background: linear-gradient(145deg, rgba(30, 32, 42, 0.7) 0%, rgba(17, 18, 24, 0.8) 100%);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }

        .glass-card:hover {
            border-color: rgba(59, 130, 246, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 10px 30px -10px rgba(14, 165, 233, 0.15);
        }

        .neon-search {
            box-shadow: 0 0 30px -5px rgba(14, 165, 233, 0.4), 
                        inset 0 0 20px -5px rgba(14, 165, 233, 0.2);
            border: 1px solid rgba(14, 165, 233, 0.5);
            animation: pulse-glow 3s infinite alternate;
        }

        @keyframes pulse-glow {
            0% { box-shadow: 0 0 20px -5px rgba(14, 165, 233, 0.3), inset 0 0 15px -5px rgba(14, 165, 233, 0.1); }
            100% { box-shadow: 0 0 40px -5px rgba(14, 165, 233, 0.6), inset 0 0 25px -5px rgba(14, 165, 233, 0.3); }
        }

        /* Animation for new cards */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px) scale(0.95); }
            to { opacity: 1; transform: translateY(0) scale(1); }
        }
        .animate-fade-in {
            animation: fadeIn 0.4s ease-out forwards;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-stagger > * {
            animation: slideIn 0.5s ease forwards;
            opacity: 0;
        }
        
        .animate-stagger > *:nth-child(1) { animation-delay: 0.1s; }
        .animate-stagger > *:nth-child(2) { animation-delay: 0.2s; }
        .animate-stagger > *:nth-child(3) { animation-delay: 0.3s; }
        .animate-stagger > *:nth-child(4) { animation-delay: 0.4s; }

        .status-dot { position: relative; }
        .status-dot::after {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            border-radius: 50%;
            background: inherit;
            animation: ping 2s cubic-bezier(0, 0, 0.2, 1) infinite;
            opacity: 0.7;
        }

        /* Class สำหรับเมนูที่ถูกเลือก (Active) */
        .nav-item.active {
            background-color: rgba(37, 99, 235, 0.1); /* bg-blue-600/10 */
            color: #60a5fa; /* text-blue-400 */
            border-color: rgba(59, 130, 246, 0.2);
            box-shadow: inset 0 0 10px rgba(59, 130, 246, 0.1);
        }

        /* --- NEW: CSS สำหรับจำกัดข้อความอธิบายให้เหลือ 2 บรรทัด --- */
        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;  
            overflow: hidden;
        }


        /* ===== FUTURE UI UPGRADE ===== */
        :root {
            --cyan: #22d3ee;
            --blue: #3b82f6;
            --violet: #8b5cf6;
            --pink: #ec4899;
            --emerald: #34d399;
            --amber: #fbbf24;
        }

        body::before,
        body::after {
            content: '';
            position: fixed;
            width: 34vw;
            height: 34vw;
            border-radius: 999px;
            filter: blur(90px);
            opacity: .16;
            pointer-events: none;
            z-index: -1;
            animation: orbDrift 14s ease-in-out infinite alternate;
        }
        body::before { background: #0ea5e9; left: 18%; top: -18%; }
        body::after { background: #8b5cf6; right: -12%; bottom: -20%; animation-delay: -5s; }

        @keyframes orbDrift {
            from { transform: translate3d(-3%, -2%, 0) scale(.95); }
            to { transform: translate3d(5%, 6%, 0) scale(1.08); }
        }

        @keyframes pageReveal {
            from { opacity: 0; transform: translateY(16px) scale(.985); filter: blur(7px); }
            to { opacity: 1; transform: translateY(0) scale(1); filter: blur(0); }
        }

        @keyframes floatY {
            0%,100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }

        @keyframes scanLine {
            0% { transform: translateY(-120%); opacity: 0; }
            12% { opacity: .35; }
            88% { opacity: .1; }
            100% { transform: translateY(800%); opacity: 0; }
        }

        @keyframes borderPulse {
            0%,100% { box-shadow: 0 0 0 1px rgba(34,211,238,.08), 0 12px 40px rgba(0,0,0,.18); }
            50% { box-shadow: 0 0 0 1px rgba(59,130,246,.26), 0 0 36px rgba(59,130,246,.10); }
        }

        @keyframes equalizer {
            0%,100% { height: 20%; }
            50% { height: 100%; }
        }

        .page-shell {
            width: 100%;
            max-width: 1440px;
            margin: 0 auto;
            animation: pageReveal .52s cubic-bezier(.2,.75,.2,1) both;
        }

        .page-hero {
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255,255,255,.08);
            background: linear-gradient(135deg, rgba(16,22,38,.88), rgba(9,11,18,.68));
            box-shadow: inset 0 1px 0 rgba(255,255,255,.04), 0 20px 70px rgba(0,0,0,.22);
            backdrop-filter: blur(22px);
        }

        .page-hero::before {
            content: '';
            position: absolute;
            inset: 0;
            background:
                radial-gradient(circle at 14% 0%, rgba(34,211,238,.15), transparent 34%),
                radial-gradient(circle at 88% 25%, rgba(139,92,246,.14), transparent 30%);
            pointer-events: none;
        }

        .page-hero::after {
            content: '';
            position: absolute;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(34,211,238,.65), transparent);
            animation: scanLine 5.8s linear infinite;
            pointer-events: none;
        }

        .holo-card {
            position: relative;
            overflow: hidden;
            background: linear-gradient(145deg, rgba(21,25,37,.76), rgba(11,13,21,.82));
            border: 1px solid rgba(255,255,255,.07);
            box-shadow: inset 0 1px 0 rgba(255,255,255,.035), 0 12px 40px rgba(0,0,0,.18);
            transition: transform .3s ease, border-color .3s ease, box-shadow .3s ease;
        }

        .holo-card::before {
            content: '';
            position: absolute;
            width: 140px;
            height: 140px;
            border-radius: 50%;
            background: rgba(34,211,238,.08);
            filter: blur(48px);
            right: -70px;
            top: -75px;
            transition: transform .4s ease, opacity .4s ease;
            pointer-events: none;
        }

        