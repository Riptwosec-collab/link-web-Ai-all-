function headers(cfg,token,extra={}){return {'apikey':cfg.anonKey,'Authorization':`Bearer ${token||cfg.anonKey}`,'Content-Type':'application/json',...extra}}
export async function signIn(cfg,email,password){const r=await fetch(`${cfg.url}/auth/v1/token?grant_type=password`,{method:'POST',headers:headers(cfg),body:JSON.stringify({email,password})});if(!r.ok)throw new Error((await r.json()).error_description||'Sign in failed');return r.json()}
export async function signUp(cfg,email,password){const r=await fetch(`${cfg.url}/auth/v1/signup`,{method:'POST',headers:headers(cfg),body:JSON.stringify({email,password})});if(!r.ok)throw new Error((await r.json()).msg||'Sign up failed');return r.json()}
export async function pullCloud(cfg,token){const get=async table=>{const r=await fetch(`${cfg.url}/rest/v1/${table}?select=*`,{headers:headers(cfg,token)});if(!r.ok)throw new Error(`Pull ${table} failed`);return r.json()};return {links:await get('links'),collections:await get('collections'),workspaces:await get('workspaces')}}
export async function pushCloud(cfg,token,payload){for(const [table,rows] of Object.entries(payload)){if(!rows?.length)continue;const r=await fetch(`${cfg.url}/rest/v1/${table}?on_conflict=id`,{method:'POST',headers:headers(cfg,token,{'Prefer':'resolution=merge-duplicates,return=minimal'}),body:JSON.stringify(rows)});if(!r.ok)throw new Error(`Push ${table} failed: ${await r.text()}`)}}
